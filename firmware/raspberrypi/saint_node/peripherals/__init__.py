"""
SAINT.OS Pi node peripheral drivers.

Each driver lives in its own module and registers via PeripheralRegistry
(see saint_node/peripheral_manager.py). Drivers implement the
PeripheralDriver base class (see base.py) — same shape as the
firmware-side peripheral_driver_t.
"""
